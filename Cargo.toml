[package]
name = "to-helper"
version = "1.2.1"
edition = "2021"
license = "MIT"
description = "Optional helper for the zsh-to directory jumper"

[profile.release]
strip = true
lto = true
codegen-units = 1

[dependencies]
